#include "Arduino.h"
#include "nexus.cpp"
//The setup function is called once at startup of the sketch
	String DA = "Help";

void setup()
{
	Nexus(0,1);
}

// The loop function is called in an endless loop
void loop()
{
	String SS = Nexus::Send(DA);
}
